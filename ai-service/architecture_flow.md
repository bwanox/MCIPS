API Layer
  receives HTTP request

Application Layer
  decides what use case to run

Domain Layer
  defines business objects and concepts

Infrastructure Layer
  implements technical details: config, preprocessing, ML, LLM, storage
